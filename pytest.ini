[pytest]
filterwarnings =
    # Ignorar advertencias de deprecación de Werkzeug y AST (Python 3.13+)
    ignore::DeprecationWarning:werkzeug
    ignore::DeprecationWarning:ast
    # Ignorar advertencias de dependencias del módulo requests
    ignore::Warning:requests